statement --- execute()  ---- select / non select 
  executeUpdate() ---- non select operation
  executeQueue() ---- select operations on 

  statement -- query independently

  batch operations ---- add batch 

  prepend statement---- 


  collable statement


  create procedure getName(in r , int, out n varchar(100))
  begin 


  use db1
  delimeter// 
  create procedure getName(in r int, out n varchar(100))
  begin 
  select name into n from student where rollNo = r;
  end//






  package JDBC;

  import java.sql.Connection;
  import java.sql.CallableStatement;
  import java.sql.DriverManager;
  import java.sql.SQLException;

  public class CallableStatementExample{
    public static void main(String args[]){
        String url = "jdbc:mysql://localhost:3306/db1";
        String username = "root";
        String password = "root";
        Connection con = DriverManager.getConnection(url,username, password);
        CallableStatement cs = con.prepareCall("{call getName(?,?)}");
        cs.setInt(1,12);
        cs.registerOutParameter(2,Types.VARCHAR);
        cs.execute();
        String name = cs.getString(2);
        System.out.println(name);
    }
  }